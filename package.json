{
  "name": "clay",
  "private": true,
  "version": "0.2.9",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:client": "bun run --cwd packages/client build && bun run --cwd packages/cli copy-client",
    "pack:publishable": "bun scripts/pack-publishable.ts",
    "publish:dry": "bun scripts/publish-from-pack.ts --dry-run",
    "publish:npm": "bun scripts/publish-from-pack.ts --publish",
    "dev": "bun run build:client && bun apps/demo/src/main.ts",
    "demo": "bun run build:client && bun apps/demo/src/main.ts",
    "demo:cli": "bun packages/cli/bin/clay apps/demo/src/examples --app -p 4000 --title \"Clay Demo\"",
    "clay": "bun packages/cli/bin/clay",
    "test": "bun test packages/core packages/ui packages/server packages/components packages/compiler packages/cli packages/persistence-file packages/persistence-redis packages/duckdb packages/kibana packages/clickhouse packages/auth packages/client/src"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/bun": "latest",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.0.0"
  },
  "type": "module"
}
