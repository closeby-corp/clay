{
  "name": "badui",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:client": "bun run --cwd packages/client build && bun run --cwd packages/cli copy-client",
    "pack:publishable": "bun scripts/pack-publishable.ts",
    "publish:dry": "bun scripts/publish-from-pack.ts --dry-run",
    "publish:npm": "bun scripts/publish-from-pack.ts --publish",
    "dev": "bun run build:client && bun apps/demo/src/main.ts",
    "demo": "bun apps/demo/src/main.ts",
    "demo:cli": "bun packages/cli/bin/badui apps/demo/src/examples --app -p 4000 --title \"BadUI Demo\"",
    "badui": "bun packages/cli/bin/badui",
    "test": "bun test packages/core packages/ui packages/server packages/components packages/cli packages/persistence-file packages/duckdb packages/kibana packages/clickhouse packages/client/src"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "type": "module"
}
