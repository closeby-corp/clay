# @badui/components

Built-in element constructors (`button`, `input`, charts, DataTable, …) for [BadUI](https://github.com/tfsoares/bad-ui).

Usually consumed via the `ui.*` facade in `@badui/ui`. See [Elements](https://github.com/tfsoares/bad-ui/blob/main/docs/elements.md).
