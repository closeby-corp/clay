# @clay/server

HTTP + WebSocket server that owns per-tab sessions and pushes element patches to the React client.

Usually started via `@clay/ui` (`ui.run`) or the `clay` CLI. See [Architecture](https://github.com/closeby-corp/clay/blob/main/docs/architecture.md).
