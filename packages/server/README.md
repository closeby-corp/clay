# @badui/server

HTTP + WebSocket server that owns per-tab sessions and pushes element patches to the React client.

Usually started via `@badui/ui` (`ui.run`) or the `badui` CLI. See [Architecture](https://github.com/tfsoares/bad-ui/blob/main/docs/architecture.md).
